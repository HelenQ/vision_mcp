# vision-mcp configuration
# Base URL of an OpenAI-compatible endpoint (DeepSeek shown as example).
BASE_URL=https://api.deepseek.com
# Your API key.
API_KEY=
# Vision model name. Defaults to deepseek-v4-flash-vision-exp when omitted.
MODEL=deepseek-v4-flash-vision-exp
